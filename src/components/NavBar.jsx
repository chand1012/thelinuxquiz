import Grid from '@geist-ui/react/Grid';

const NavBar = (props) => {
    return (
        <Grid.Container gap={2} justify="left">
            <Grid xs={12}>
                
            </Grid>
        </Grid.Container>
    );
};

export default NavBar;